name: import public Telegram

on:
  schedule:
    - cron: "7-57/5 * * * *"
  workflow_dispatch:

concurrency:
  group: public-telegram-import
  cancel-in-progress: false

jobs:
  import:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Import public Telegram directly into Supabase
        env:
          PODRABOTKA154_SUPABASE_URL: ${{ secrets.PODRABOTKA154_SUPABASE_URL }}
          PODRABOTKA154_SUPABASE_KEY: ${{ secrets.PODRABOTKA154_SUPABASE_ANON_KEY || secrets.PODRABOTKA154_SUPABASE_SERVICE_ROLE_KEY }}
        run: npm run import-public
