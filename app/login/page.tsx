import { ActionForm } from '@/components/action-form';
import { loginAction, resetPassword } from '@/app/actions';
export const metadata = { title: 'Войти', robots: { index: false, follow: false } };
export default function Login() { return <section className="panel"><h1>Один аккаунт для работы и найма</h1><p className="lead">Сохраняйте объявления или размещайте свои вакансии.</p><ActionForm action={loginAction} label="Продолжить"><label>Email<input name="email" type="email" required autoComplete="email" placeholder="you@example.org"/></label><label>Пароль<input name="password" type="password" required minLength={10} maxLength={128} autoComplete="current-password"/><small>Не меньше 10 символов.</small></label><label>Что сделать?<select name="mode"><option value="login">Войти</option><option value="signup">Зарегистрироваться</option></select></label><p className="small muted">При регистрации подтвердите email. Для публичного запуска администратор должен настроить SMTP.</p></ActionForm><details><summary>Забыли пароль?</summary><ActionForm action={resetPassword} label="Отправить письмо"><label>Email<input name="email" type="email" required/></label></ActionForm></details></section>; }
